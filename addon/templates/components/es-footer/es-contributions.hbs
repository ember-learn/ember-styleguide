
{{#if hasBlock}}
  {{yield}}
{{else}}
  {{#each contributorLinks as |link|}}
    <div class="contributor">
      <p>{{link.name}}</p>
      <a href={{link.href}}>
        {{svg-jar link.class class="contributor-logo"}}
      </a>
    </div>
  {{/each}}
{{/if}}
